/*
setDuration=40
breakDuration=30
totalSeconds=70
totalsetDuration=setDuration+breakDuration


if totalSeconds<=setDuration  
    then set color "red"  


if totalSeconds>setDuration but less thn totalSetDuration
    then set color "green"

if totalSeconds <=totalSetDuration+setDuration then set color "red"

if totalSeconds >

[40,30,40,30,40,30,40,30]

*/

